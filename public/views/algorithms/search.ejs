<!DOCTYPE html>
<!-- Daniel Isler -->
<html lang = "de">
  <head>
    <meta charset="utf-8"/>
    <title>Suchen</title>
    <link rel="shortcut icon" type="image/x-icon" href="/imgs/favicon.ico"/>
    <link href="https://fonts.googleapis.com/css?family=Righteous" rel="stylesheet">
    <link rel="stylesheet" href="/styles/course.css">
    <link rel="stylesheet" href="/styles/navigation.css">
  </head>
  <body>
    <%- include(partialsPath + 'header.ejs') %>
    <main>
      <div class = "title">
        <h1>Such-Algorithmen</h1>
      </div>
      <div>
        <p>Suchalgorithmen werden dazu verwendet, um den Index eines Elementes mit dem gesuchten Wert aus einer sortierten Liste herauszufinden. Es wird also ein gewisser Wert aus einer Liste herausgesucht. Eine Anwendung für einen Suchalgorithmus ist zum Beispiel in einem Telefonbuch einen Namen herauszusuchen.</p>
      </div>
      <div class = "searchingalgorithms">
        <% for (let index = courses[0].indexOf('Suchalgorithmen') + 1; index < courses[3].indexOf(courses[3][courses[0].indexOf('Suchalgorithmen')], courses[0].indexOf('Suchalgorithmen') + 1); index++) { %>
        <div id = "<%= courses[1][index].split('/')[courses[1][index].split('/').length - 1]%>">
          <h2><a <% if (!courses[4][index]) { %> class="unavailable" <% } %> href="<%= courses[1][index] %>"> <%= courses[0][index] %></h2>
            <img src = "/imgs/algorithms/search/<%= courses[1][index].split('/')[courses[1][index].split('/').length - 1]%>.png" alt = "<%= courses[0][index] %>" class = "pics"/>
          </a>
        </div>
        <% } %>
      </div>
    </main>
    <%- include(partialsPath + 'footer1.ejs') %>
  </body>
</html>
