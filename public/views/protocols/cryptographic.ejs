<!DOCTYPE html>
<!-- Daniel Isler -->
<html lang = "de">
  <head>
    <meta charset="utf-8"/>
    <title>Verschlüsselungsprotokolle</title>
    <link rel="shortcut icon" type="image/x-icon" href="/imgs/favicon.ico"/>
    <link href="https://fonts.googleapis.com/css?family=Righteous" rel="stylesheet">
    <link rel="stylesheet" href="/styles/course.css">
    <link rel="stylesheet" href="/styles/navigation.css">
  </head>
  <body>
    <%- include(partialsPath + 'header.ejs') %>
    <main>
      <div class = "title">
        <h1>Verschlüsselungs-Protokolle</h1>
      </div>
      <div>
        <p>Verschlüsselungsprotokolle garantieren eine verschlüsselte Datenübertragung in einem Rechennetz. Mit einem Schlüsselübergabeprotokoll und symmetrischen Verfahren wird die Integrität (Richtigkeit, Originalität) und die Vertraulichkeit der übertragenen Nachrichten sichergestellt.</p>
      </div>
      <div class = "cryptographic">
        <% let index = courses[0].indexOf('Verschlüsselungsprotokolle') + 1; while (courses[3][index] > courses[3][courses[0].indexOf('Verschlüsselungsprotokolle')]) { %>
        <% if (courses[3][index] == courses[3][courses[0].indexOf('Verschlüsselungsprotokolle') + 1]) { %>
        <div class = "courseLink">
          <a <% if (!courses[4][index]) { %> class="unavailable" <% } %> href="<%= courses[1][index] %>"> <h2><%= courses[0][index] %></h2>
            <img src = "/imgs/protocols/cryptographic/<%= courses[1][index].split('/')[courses[1][index].split('/').length - 1] %>.png" alt = "<%= courses[0][index] %>" class = "pics"/>
          </a>
        </div>
        <% } %>
        <% index += 1 } %>
      </div>
    </main>
    <%- include(partialsPath + 'footer1.ejs') %>
  </body>
</html>
